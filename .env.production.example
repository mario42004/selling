# Dominio cuyo DNS apunta a la IP pública de la máquina remota.
N8N_DOMAIN=pedidos.example.com
TZ=Europe/Madrid

# Genera valores largos y distintos para producción.
MARIADB_DATABASE=whatsapp_orders
MARIADB_USER=orders_app
MARIADB_PASSWORD=change-me-with-a-long-random-password
MARIADB_ROOT_PASSWORD=change-me-with-another-long-random-password
N8N_ENCRYPTION_KEY=change-me-with-at-least-32-random-characters
